#!/usr/bin/env ruby

require_relative '../lib/move.rb'

# Code your CLI Here

puts "Welcome to Tic Tac Toe!"
puts "Where would you like to go?"
input = gets.strip

index = input_to_index(input) #why do you need this?
board = [" "," "," "," "," "," "," "," "," "]
move(board, index)
display_board(board)

#def newMethod(1arg, 2arg)
#return 1arg
#end

#beth = Person.new(name: Beth)
#newMethod(Beth, Traci) => Beth
